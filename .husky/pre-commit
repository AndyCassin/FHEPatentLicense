#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run lint-staged
npx lint-staged

# Security check
echo "🔒 Running security audit..."
npm audit --audit-level=high || echo "⚠️  Security audit found issues. Review before committing."

echo "✅ Pre-commit checks complete!"
